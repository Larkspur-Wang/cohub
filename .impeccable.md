# Cohub Design Context

## Product Purpose

Cohub 是面向开发者和技术团队的 AI agent workspace。用户在浏览器中托管可复用的 space，运行云端 agent，调试会话，查看 sandbox 生命周期，管理 space、channel、文件、任务和自动化流程。

核心体验应像一个专业的工程控制台：高密度、可追踪、低噪音、响应迅速，让用户相信系统可靠且可控。

## Users

- 开发者、技术团队、AI 工程师和平台构建者。
- 熟悉 DevTool、CLI、云服务和协作工具。
- 对信息密度、状态可见性、操作效率、界面一致性有较高要求。
- 更偏好直接、精确、可解释的界面，而不是营销化或装饰化表达。

## Core Use Cases

- 创建和管理 space。
- 与 AI agent 进行会话，观察工具调用、流式输出和任务进度。
- 查看和管理 sandbox、文件、端口、checkpoint、cron job、task run。
- 配置 channel、SSH key、规则、成员、邀请和环境变量。
- 在复杂上下文中快速定位状态、异常、历史记录和下一步操作。

## Brand Personality

- **三个词**：硬核、精确、高效
- **Voice**：专业、直接、不啰嗦。像一个靠谱的工程搭档。
- **Emotional goals**：让用户感到“这是专业人士用的工具”——精确、可靠、高效。

## Aesthetic Direction

- **风格**：技术硬核 + 简约高级。参考 Linear、Vercel Dashboard、Raycast 的克制感和精度感，但要有 Cohub 自己的识别度。
- **主题**：Dark mode 优先，支持 Light mode。主题必须通过 semantic theme tokens 表达，不能在组件里用系统主题媒体查询绕过用户选择。
- **品牌色**：`#FF3E00` 是品牌基准色，但 UI 实现里应使用 `brand` / `brand-*` 语义 token，不直接硬编码 hex。
- **字体**：使用 Geist / Geist Mono，保持工程工具的清晰、现代和克制。
- **视觉节奏**：高密度但不拥挤；用字号、字重、间距、对齐和少量色彩建立层级，少用装饰性容器。
- **明确不要**：gradient text、glassmorphism、glowing neon accents、generic cyan/purple AI palette、Inter 字体、圆角卡片网格模板。
- **参考反例**：不要像 generic AI dashboard template。

## Design Principles

1. **Precision over decoration** — 每个像素都有意义，不用装饰性元素填充空间。
2. **Information density with clarity** — 开发者工具需要高密度信息，但必须层次分明。
3. **Brand cohesion** — 品牌色应作为交互状态、active 指示、重点元素的统一语言；组件内使用 theme semantic token，而不是原始颜色值。
4. **Typographic hierarchy** — 用字重、字号、行高和密度建立层次，减少对边框和背景块的依赖。
5. **Functional minimalism** — 去掉所有不服务于核心任务的东西，但保留必要状态、上下文和可追踪信息。
6. **State visibility** — 运行中、失败、等待、完成、同步、加载、禁用等状态必须清晰可见且语义一致。
7. **Calm confidence** — 即使处理复杂 agent 输出和工具调用，也要保持界面稳定、克制、可预测。

## Theme System

Cohub Web 的样式系统以 semantic theme tokens 为核心：

```txt
apps/web/src/styles/
  theme.css
  themes/
    dark.css
    light.css
```

- `theme.css` 负责导入具体 theme，并通过 Tailwind v4 `@theme` 暴露语义 token。
- 每个 theme 独立定义一份完整变量。新增 theme 时应新增独立文件，而不是在组件里写一次性条件样式。
- 组件应优先使用 `bg-bg-*`、`text-text-*`、`border-border-*`、`bg-brand`、`text-brand-contrast-fg`、`bg-overlay-*`、`text-error-*` 等语义 class。
- 不要在组件中硬编码 `#FF3E00`、`text-white`、`bg-black/*`、`bg-white/*`、`amber-*`、`cyan-*`、`indigo-*` 等原始颜色，除非它是无法 token 化的外部内容。
- 所有 hover、active、selected、focus、disabled、loading、error 状态都应通过语义 token 表达，保证 light / dark / future themes 可以独立校准。

### Foreground Token Boundaries

前景色 token 要按背景语义使用，避免一个 token 到处复用：

- `text-brand-contrast-fg`：用于 `bg-brand` / 强品牌按钮或徽标。
- `text-brand-muted-fg`：用于品牌弱背景，例如 `bg-brand-bg`、`bg-brand-muted`。
- `text-overlay-control-text`：用于 lightbox、drawer、modal 等 overlay 控件。
- `text-error-fg`：用于 `bg-error` 这类强错误背景。
- 普通界面文字继续使用 `text-text-primary`、`text-text-secondary`、`text-text-tertiary`、`text-text-placeholder`。

### Theme-safe Interaction Patterns

- Overlay / modal / drawer 使用 `bg-overlay-scrim`、`bg-overlay-scrim-strong`、`bg-overlay-control` 等 token。
- Provider 品牌色使用 provider token，例如 `provider-discord`、`provider-feishu`，不要直接使用第三方品牌 hex。
- 数据可视化使用专门 token，例如 heatmap 使用 `heatmap-*`，不在组件里写 `oklch(...)` 或 `prefers-color-scheme`。
- Focus ring、active indicator、selected background 应优先使用品牌或边框语义 token，保持精确但不过度发光。

## Component & Interaction Guidelines

- Buttons 要有明确层级：primary 用品牌色，secondary / ghost / text action 用低干扰样式。
- 不要让每个按钮都像 primary；高频操作和危险操作要有清晰区分。
- 表单、输入框、选择器应保持紧凑、清晰、可键盘操作，focus 状态要可见。
- 空状态要帮助用户理解下一步，而不是只说“暂无数据”。
- 错误信息要直接说明发生了什么，以及用户能做什么。
- 流式内容、工具调用和后台任务要优先保证可读性和稳定性，动画只用于表达状态，不制造噪音。
- 列表和侧栏应保持扫描效率：对齐、截断、状态点、时间和标题层级要一致。

## Motion

- 动效应短促、克制、表达状态变化。
- 优先使用 opacity / transform，避免 layout 属性动画。
- 尊重 `prefers-reduced-motion`。
- 不使用 bounce / elastic 这类过度拟物或过时缓动。

## Writing Style

- 文案专业、直接、短句优先。
- 避免营销腔、夸张词和重复说明。
- 中文和英文 / 数字之间保留合适空格。
- 技术概念可直接使用英文，例如 space、agent、sandbox、channel、checkpoint、task run。
