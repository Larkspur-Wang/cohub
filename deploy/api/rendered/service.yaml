apiVersion: v1
kind: Service
metadata:
  name: netaverses-api
  namespace: netaverses
  labels:
    app.kubernetes.io/name: netaverses-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: netaverses-api
  ports:
    - name: http
      port: 8787
      targetPort: 8787
