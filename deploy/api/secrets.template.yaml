# Workspace API 敏感配置模板
# 注意：请勿提交真实 secrets.yaml 到仓库

apiVersion: v1
kind: Secret
metadata:
  name: netaverses-api-secrets
  namespace: netaverses
stringData:
  # Redis 连接地址
  REDIS_URL: "redis://redis-service:6379"
  # 可选：访问私有仓库时使用
  gitea-token: "CHANGE_ME_OR_EMPTY"
