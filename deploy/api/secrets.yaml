# 请填写后再部署
# 如需重新生成可参考 secrets.template.yaml

apiVersion: v1
kind: Secret
metadata:
  name: netaverses-api-secrets
  namespace: netaverses
stringData:
  # Redis 连接地址
  REDIS_URL: "redis://redis-service:6379"
  # 可选：访问私有仓库时使用；如果目前只读 public 仓库可以留空字符串
  gitea-token: "CHANGE_ME_OR_EMPTY"
