# Template — fill in before deploying
apiVersion: v1
kind: Secret
metadata:
  name: cohub-worker-secrets
  namespace: cohub
type: Opaque
stringData:
  DATABASE_URL: "__DATABASE_URL__"
  REDIS_URL: "__REDIS_URL__"
  WORKER_SECRET: "__WORKER_SECRET__"
