apiVersion: v1
kind: ConfigMap
metadata:
  name: {{APP_NAME}}-config
  namespace: {{NAMESPACE}}
data:
  ENV: "{{ENV}}"
  REDIS_URL: "{{REDIS_URL}}"
  SESSIONS_DIR: "{{SESSIONS_DIR}}"
  SANDBOX_WS_HOST: "0.0.0.0"
  SANDBOX_WS_PORT: "{{PORT}}"
