---
"@neta-art/cohub": patch
---

Support direct generation mode in create-space prompts: optional `mode` and `generation` fields on `CreateSpacePromptInput`, optional `content`, and `execution` kind on immediate responses.
