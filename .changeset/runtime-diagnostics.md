---
"@neta-art/cohub": minor
"@neta-art/cohub-cli": patch
---

Add redacted, local-only Runtime diagnostics with bounded JSONL rotation, incremental `runtime logs`, sandboxd output capture, stable runtime identity, and request/trace correlation across the Agent, Gateway, API, and Web status surfaces.

增加脱敏且仅保存在本机的 Runtime 诊断能力：有界 JSONL 轮转、增量 `runtime logs`、sandboxd 输出采集、稳定 Runtime ID，以及贯通 Agent、Gateway、API 和 Web 状态页的 request/trace 关联。
