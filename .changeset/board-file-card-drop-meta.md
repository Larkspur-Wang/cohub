---
"@neta-art/cohub": patch
---

Board file cards drop the `TYPE · size` meta line; the title, excerpt and category stripe already carry enough to recognise a file. The `fileMetaLine` / `formatFileSize` helpers remain available for custom card renderers.
