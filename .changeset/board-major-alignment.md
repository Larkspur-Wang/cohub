---
"@neta-art/cohub-cli": major
---

Align the CLI with the Board domain rename in `@neta-art/cohub` 3.0.0. The CLI now targets the Board transaction and playback APIs; canvas-era SDK surfaces it depended on are gone.
