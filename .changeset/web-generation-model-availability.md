---
"@neta-art/cohub": patch
---

The web model picker shows availability dots for generation models too, not just LLMs — the same status feed now marks unavailable or degraded image/video/audio models in the picker, in both the collapsed row and the expanded model list.
